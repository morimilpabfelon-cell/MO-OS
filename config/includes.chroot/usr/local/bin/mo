#!/usr/bin/env bash
set -Eeuo pipefail

release_file=/etc/mo-release
machine_name=mo-dev

show_help() {
  cat <<'EOF'
MO OS command interface

Usage: mo <command>

Commands:
  status      Show MO OS and kernel status
  doctor      Validate the terminal foundation
  dev-init    Create the pinned Arch development domain
  dev         Open a shell in the Arch development domain
  dev-status  Show development-domain status
  install     Show disk-installer status
  help        Show this help
EOF
}

status() {
  [[ -r "$release_file" ]] && cat "$release_file"
  printf 'KERNEL="%s"\n' "$(uname -r)"
  printf 'VIRTUALIZATION="%s"\n' "$(systemd-detect-virt 2>/dev/null || echo none)"
}

doctor() {
  local failures=0
  for command_name in bash git curl systemd-nspawn machinectl sha256sum tar zstd; do
    if command -v "$command_name" >/dev/null 2>&1; then
      printf '[ok] %s\n' "$command_name"
    else
      printf '[missing] %s\n' "$command_name" >&2
      failures=$((failures + 1))
    fi
  done
  [[ -r "$release_file" ]] || { echo '[missing] /etc/mo-release' >&2; failures=$((failures + 1)); }
  if ((failures > 0)); then
    echo "MO OS foundation has $failures missing requirement(s)." >&2
    return 1
  fi
  echo 'MO OS terminal foundation is healthy.'
}

dev_init() {
  exec sudo /usr/local/sbin/mo-dev-init
}

dev_shell() {
  [[ -d "/var/lib/machines/$machine_name" ]] || {
    echo 'Arch development domain is not initialized. Run: mo dev-init' >&2
    exit 1
  }
  sudo machinectl start "$machine_name" >/dev/null 2>&1 || true
  exec sudo machinectl shell "root@${machine_name}"
}

dev_status() {
  if [[ -d "/var/lib/machines/$machine_name" ]]; then
    machinectl status "$machine_name" || true
  else
    echo 'Arch development domain: not initialized'
  fi
}

installer_status() {
  cat <<'EOF'
Disk installation is disabled in Alpha 0.1.
MO OS must first pass installer tests on disposable virtual disks.
EOF
  return 2
}

case "${1:-help}" in
  status) status ;;
  doctor) doctor ;;
  dev-init) dev_init ;;
  dev) dev_shell ;;
  dev-status) dev_status ;;
  install) installer_status ;;
  help|-h|--help) show_help ;;
  *) echo "Unknown command: $1" >&2; show_help >&2; exit 2 ;;
esac
